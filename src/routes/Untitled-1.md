


middlewares
    - common logic through out routes

routes -
    - defines api endpoints for the applicatoin
        - ie userRouter
            - /users
            - users/:userid

services:
    - defines business logic
    